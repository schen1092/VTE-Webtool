<div class="center jumbotron">
  <h1>Welcome to the VTE Prophylaxis Compliance</h1>

<div class="table-responsive">
            <table class="table table-striped table-bordered table-hover">

              <tbody class="rowlink" data-link="row">
                <tr>
                  <th>Provider ID</th>
                  <th>Name</th>
                  <th>JHED ID</th>
                  <th>Role</th>
                  <th colspan="1"></th>
                </tr>
			  <% @providers.each do |provider| %>
			    <tr>
			      <td><%= link_to provider.ORDERING_PROV_JHED_ID, provider_path(provider) %></td>
			      <td><%= provider.ORDERING_PROVIDER %></td>
			      <td><%= provider.ORDERING_PROV_JHED_ID %></td>
			      <td><%= provider.ORDERING_PROVIDER_ROLE %></td>
            <td><%= form_tag import_provider_orders_path(provider), multipart: true do %>
                  <%= file_field_tag :file %>
                  <%= submit_tag "Import" %>
                <% end %></td>
			    </tr>
			  <% end %>
              </tbody>
            </table>
  </div>

  <h2>Import Providers</h2>

  <%= form_tag import_providers_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag "Import" %>
  <% end %>
</div>

<%= link_to image_tag("rails.png", alt: "Rails logo"),
            'http://rubyonrails.org/' %>